pipeline {
  agent any
  stages {
      
    stage('Build image') {
        steps {
            sh "docker build -t nginx-server:22 01_automation_test/Dockerfile"
        }
    }
    
    stage('Run server') {
        steps {
            sh "docker run -d -p 80:80 nginx-server:22"
        }
    }
  }
}
